import React from 'react';
import { Story, Meta } from '@storybook/react/types-6-0';
import { {{headCaps name}}, {{headCaps name}}Props } from '../../components/{{name}}';

export default {
  title: 'Elements/{{headCaps name}}',
  component: {{headCaps name}},
} as Meta;

const Template: Story<{{headCaps name}}Props> = (args) => <{{headCaps name}} {...args} />;

export const Primary = Template.bind({});
Primary.args = {
  color: 'primary',
  size: 'normal',
};
